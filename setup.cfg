[sqla_testing]
requirement_cls=IfxAlchemy.requirements:Requirements
profile_file=.profiles.txt

[db]
# The official SQLAlchemy suite should be run through run_tests.py, which reads
# INFORMIX_SQLALCHEMY_SUITE_URL first and falls back to INFORMIX_SQLALCHEMY_URL.
# Keep this file free of real credentials.
sqlite=sqlite:///:memory:
