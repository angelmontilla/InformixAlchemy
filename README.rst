IfxAlchemy
==========

The maintained project documentation is in ``README.md``.

This package's supported SQLAlchemy backend is ``informix+pyodbc``. The legacy
IfxPy module remains importable for source compatibility but is not a published
entry point.
